

Cron jobs are performed in PostgreSQL primarily through the use of an officially maintained extension called **`pg_cron`**. This extension allows you to schedule SQL commands or functions to run periodically directly within your database environment.

- ==learn about postgres cron jobs==

