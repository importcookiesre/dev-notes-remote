


## 1. remote : 

1. check remote url : `git remote -v`
2. remove remote url : `git remote remove origin`
3. add origin url : 
	1. `git remote set-origin origin <url>`
	2. `git remote add origin https://github.com/surajgoraicse/unlimitted-dns.git   `


```env
dsfs=sdfds
```
